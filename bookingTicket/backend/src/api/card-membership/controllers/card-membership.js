'use strict';

/**
 * card-membership controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::card-membership.card-membership');
